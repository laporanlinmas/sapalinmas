// ─── Public API ───────────────────────────────────────────────────────────────
export { default }          from './ChatBot';
export { Loading }          from './steps';
export { defaultTheme }     from './theme';
export type {
  ChatBotProps,
  ChatStep,
  StepOption,
  ChatTheme,
  RenderedStep,
  SpeechSynthesisOptions,
  FloatingStyle,
} from './types';
